fn main() {
    var i byte
    var n byte = 0

    cls()

    for i = 0 to 9 {
        putstr(0, i, "COUNTED FOR")
    }

    for n < 3 {
        putstr(16, n, "COND FOR")
        n = n + 1
    }

    for {
        putstr(0, 12, "INFINITE FOR + BREAK")
        break
    }

    gotoxy(0, 22)
}
