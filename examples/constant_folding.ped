const BORDER = $d020

fn main() {
    var x int = 2 + 3 * 4
    var color byte = (%1111_0000 & 15) + 1

    cls()

    poke(BORDER + 1, color)

    putstr(0, 0, "CONST FOLD")
    putstr(0, 1, itoa(x))

    gotoxy(0, 22)
}
